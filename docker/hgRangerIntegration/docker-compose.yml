#
# Licensed to the Apache Software Foundation (ASF) under one or more
# contributor license agreements.  See the NOTICE file distributed with
# this work for additional information regarding copyright ownership.
# The ASF licenses this file to You under the Apache License, Version 2.0
# (the "License"); you may not use this file except in compliance with
# the License.  You may obtain a copy of the License at
#
#    http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
services:

  ranger-solr:
    image: solr:8.11
    container_name: ranger-solr
    ports:
      - "8983:8983"
    environment:
      SOLR_JAVA_MEM: "-Xms512m -Xmx512m"
    volumes:
      - ./solr-init.sh:/solr-init.sh:ro
      - ./ranger-audits-conf:/ranger-audits-conf:ro
    entrypoint: ["/solr-init.sh"]
    healthcheck:
      test: ["CMD-SHELL", "curl -s 'http://localhost:8983/solr/admin/cores?action=STATUS&core=ranger_audits&wt=json' | grep -q '\"status\":0' || exit 1"]
      interval: 10s
      start_period: 60s
      retries: 10

  ranger-db:
    image: postgres:14
    container_name: ranger-db
    environment:
      POSTGRES_DB: ranger
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: rangeradmin
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d ranger"]
      interval: 10s
      retries: 10

  ranger:
    build:
      context: ../..
      dockerfile: docker/hgRangerIntegration/Dockerfile.ranger
    image: hugegraph-ranger-admin-local:2.9.0
    container_name: ranger
    depends_on:
      ranger-db:
        condition: service_healthy
      ranger-solr:
        condition: service_healthy
    environment:
      # apache/ranger:2.9.0's entrypoint (ranger.sh) reads these three vars directly
      # (db_root_password/db_user/db_password in install.properties) — the legacy
      # RANGER_ADMIN_DB_* names below are kept for the parts of the image that still
      # read them, but POSTGRES_PASSWORD/RANGER_DB_USER/RANGER_DB_PASSWORD are what
      # actually wires up the postgres connection on this image version.
      # RANGER_DB_PASSWORD also becomes rangerAdmin_password (the "admin" UI login
      # password) via the same entrypoint, so it must stay "rangerR0cks!" to match
      # the credentials documented in ranger-integration-test-guide.md and used by
      # the image's own create-ranger-services.py bootstrap script.
      POSTGRES_PASSWORD: rangeradmin
      RANGER_DB_USER: rangeradmin
      RANGER_DB_PASSWORD: rangerR0cks!
      RANGER_ADMIN_DB_HOST: ranger-db
      RANGER_ADMIN_DB_NAME: ranger
      RANGER_ADMIN_DB_USER: rangeradmin
      RANGER_ADMIN_DB_PASS: rangeradmin
      RANGER_ADMIN_DB_ROOT_USER: postgres
      RANGER_ADMIN_DB_ROOT_PASS: rangeradmin
      RANGER_ADMIN_HTTP_PORT: "6080"
      RANGER_AUDIT_SOLR_URLS: "http://ranger-solr:8983/solr/ranger_audits"
    ports:
      - "6080:6080"
    healthcheck:
      test: ["CMD-SHELL", "wget -q --server-response -O/dev/null http://localhost:6080/login.jsp 2>&1 | grep -q 'HTTP/1.1 200' || exit 1"]
      interval: 15s
      start_period: 180s
      retries: 20

  hugegraph:
    build:
      context: ../..
      dockerfile: docker/hgRangerIntegration/Dockerfile.hugegraph
    image: hugegraph-ranger-local:1.7.0
    container_name: hugegraph
    depends_on:
      ranger:
        condition: service_healthy
    ports:
      - "8080:8080"
    volumes:
      - ./ranger-hugegraph-security.xml:/hugegraph-server/conf/ranger-hugegraph-security.xml:ro
      - ./rest-server.properties:/hugegraph-server/conf/rest-server.properties:ro
      - ./hugegraph.properties:/hugegraph-server/conf/graphs/hugegraph.properties:ro
